[project]
name = "resumatch-adk"
version = "0.1.0"
description = "Agentic Resume Intelligence Pipeline using Google ADK"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.4",
]
